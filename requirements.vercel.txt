# Vercel-Optimized Requirements - Serverless Functions
# Lighter dependencies that work better in serverless environment

# Core FastAPI and server
fastapi==0.115.5
pydantic==2.11.3
pydantic-settings==2.9.1

# HTTP client - lighter version
httpx==0.24.1
python-dotenv==1.1.0

# Database - try newer supabase (serverless handles dependencies better)
supabase==2.13.0

# Essential only - keep it minimal for serverless
python-multipart==0.0.2
typing-extensions==4.13.2

# Async support
anyio==4.9.0

# JSON handling
jsonschema==4.21.1

# Security (required)
cryptography>=3.4.8

# NOTE: Removed heavy dependencies that might cause issues:
# - livekit (too heavy for serverless)
# - aiohttp (not needed if using httpx)
# - psutil (system-level, not needed in serverless)
