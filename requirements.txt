# Vercel-Optimized Requirements - MINIMAL FOR SERVERLESS
# Avoid six.moves._thread issues entirely

# Core FastAPI and server  
fastapi==0.115.5
pydantic==2.11.3

# ASGI adapter for serverless (CRITICAL)
mangum==0.17.0

# HTTP client - minimal version
httpx==0.24.1
python-dotenv==1.1.0

# Database - try older supabase to avoid storage3
supabase==0.7.1

# Minimal essentials only
python-multipart==0.0.2
typing-extensions==4.13.2

# NOTE: Removed ALL heavy dependencies for serverless:
# - No livekit, aiohttp, psutil, cryptography, anyio, jsonschema
# - No pydantic-settings (use basic settings)
# - Minimal footprint to avoid import errors
